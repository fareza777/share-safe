<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.ShareSafe" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/window_background</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- The first frame is the brand gradient, so light status icons read correctly there;
             ShareSafeTheme overrides both flags as soon as Compose takes over. -->
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
